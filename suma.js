// suma.js – versión corregida

function suma(a, b) {
  // Se corrige el operador: ahora realizamos la suma
  return a + b;
}

const resultado = suma(30, 20);
console.log('Resultado:', resultado); // Debería imprimir 50
